import React from 'react'

let Link = (props) => {
    return (
    <React.Fragment>
        {props.children}
        <React.Fragment> </React.Fragment>
    </React.Fragment>
)
}

export default Link
